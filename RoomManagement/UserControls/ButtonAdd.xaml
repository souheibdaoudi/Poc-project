<UserControl x:Class="RoomManagement.UserControls.ButtonAdd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="buttonAdd">

    <Button Style="{StaticResource addButton}" Width="200" VerticalAlignment="Top" HorizontalAlignment="Right" Tag="{Binding RoomID}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Caption}" VerticalAlignment="Center"/>
            <Border Margin="20 0 0 0" Width="35" Padding="1" Background="#3253F1" CornerRadius="50">
                <Image Source="../Images/plus (1).png" />
            </Border>
        </StackPanel>
    </Button>
    
</UserControl>